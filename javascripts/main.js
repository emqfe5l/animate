 window.onload = function () {
   animate && animate.train && animate.train.init();
   animate && animate.letter && animate.letter.init();
   animateUnit && animateUnit.unit && animateUnit.unit.init();
 };
